{
    "name": "oleg-starter-pack",
    "version": "4.2.0",
    "description": "Some handy base Sass and utility classes for use in projects.",
    "license": "MIT",
    "author": "Sam Hardacre <me@nocturnalmonkey.com>",
    "main": "index.js",
    "scripts": {
        "preversion": "git checkout develop",
        "postversion": "git push --tags origin develop && git checkout master && git pull --tags origin develop && git push --tags origin master && git checkout develop",
        "lint:scss": "stylelint --rd --rdd \"**/*.{css,scss,sass}\" -s scss",
        "fix:scss": "stylelint --rd --rdd \"**/*.{css,scss,sass}\" -s scss --fix"
    },
    "repository": "git@github.com:AgePartnership/oleg-starter-pack.git",
    "engines": {
        "node": ">=12.13.0"
    },
    "devDependencies": {
        "ap-stylelint-config": "github:agepartnership/ap-stylelint-config#v1.2.0"
    }
}
